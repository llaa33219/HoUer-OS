#!/usr/bin/env python3

import sys
import os

# Add the script's directory to the Python path to find the houer_manager module
sys.path.insert(0, os.path.dirname(__file__))

from houer_manager.main import main

if __name__ == "__main__":
    main() 